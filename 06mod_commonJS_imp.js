const fs = require("./mod_export")

fs()
// fs.abc()

/* this kind of importing modules which were imported using require keyoword &      exported using modules.export is known as common js modules. If the Extension of the file to be imported is .mjs
Then we cant use require because .mjs is reserved for ES modules & they are 
 imported in a different Way
*/